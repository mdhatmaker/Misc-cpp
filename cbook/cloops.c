#include <stdio.h>

int main()
{
    /* If-Else
    if (expression)
        statement;
    else
        statement;
    */

    /* Else-If
    if (expression)
        statement;
    else if (expression)
        statement;
    else if (expression)
        statement;
    else
        statement;
    */

    // Switch

    // While Loop

    // For Loop

    // Do-While Loop

    // Break and Continue

    // Goto and labels

    
    
}